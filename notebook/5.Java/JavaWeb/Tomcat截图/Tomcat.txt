1、Tomcat是一个Servlet容器，Servlet容器是用来运行Servlet的，
运行Servlet的前提是，要将从客户端接收的HTTP请求报文封装成ServletRequest请求对象，
并将ServletResponse对象序列化为HTTP响应报文，并发送至客户端。


2、Tomcat的两个主要模块：连接器(Connector)和容器(Container)

3、Socket是操作系统提供给应用程序的一个编程接口，
通过这个接口可以实现“两个不同计算机上的两个应用可以通过Socket发送和接受字节流”













